"""Core config, geometry, state, and integration helpers for sim1d.

Members are imported by their own module path -- this package deliberately
re-exports nothing, so there is exactly one name for every symbol. The
configuration templates and loaders that ``config`` defines are additionally
published on the package surface in ``cablp.solvers._sim1d``.
"""
